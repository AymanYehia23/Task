import 'package:flutter/material.dart';

class Strings {
  static const String taskBox = 'myBox';
  static const String appTitle = 'Task';
}

class Sizes {
  static const mobileDesignSize = Size(360, 640);
  static const desktopDesignSize = Size(1280, 832);
}

class AppTheme {
  static const Color themePrimaryColor = Color.fromRGBO(0, 202, 93, 1);
}
